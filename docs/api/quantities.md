# Quantity Classes

All 41 concrete quantity types. Each is a thin subclass of
{class}`~engunits.base.BaseQuantity` that sets `_quantity_type` to
link it to its SI and Imperial default units.

```{eval-rst}
.. automodule:: engunits.quantities
   :members:
   :undoc-members:
```
